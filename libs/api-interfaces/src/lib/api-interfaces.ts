export interface Message {
    message: string;
}

export interface Candidate {
    name: string;
}

export interface Trait {
    name: string;
    id: number;
}

export interface Consent {
  name: string;
}